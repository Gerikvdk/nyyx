document.addEventListener("visibilitychange", function() {
    if (document.visibilityState === "hidden") {
        var event = new Event("beforePageExit");
        document.dispatchEvent(event);
    }
});